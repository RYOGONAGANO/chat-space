.content-leftside
  .content-leftside__upper
    .content-leftside__upper__text
      = current_user.name
    .content-leftside__upper__icon
      %ul.icons
        %li.list
          = link_to new_group_path do
            = fa_icon 'pencil-square-o', class: 'icon'
        %li.list
          = link_to edit_user_path(current_user) do
            = fa_icon 'cog', class: 'icon'
  .groups
    .group
      - current_user.groups.each do |group|
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。